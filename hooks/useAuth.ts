'use client';

/**
 * useAuth Hook
 * 
 * Convenience hook for accessing AuthContext
 * Re-exports useAuth from AuthContext for consistent imports
 */

export { useAuth } from '../contexts/AuthContext';
export type { AuthContextValue } from '../contexts/AuthContext';
